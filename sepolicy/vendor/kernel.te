dontaudit kernel self:capability kill;
allow kernel init:process noatsecure;
allow kernel self:security setenforce;
allow kernel tmpfs:dir search;
allow kernel vfat:file open;
allow kernel self:capability { dac_override kill };
allow kernel kernel:capability { kill };
allow kernel kernel:socket { create };
allow kernel vfat:dir { search };
